@page "/counter"
@*@inject IDataAccess _db*@
@inject IStartConfig _startConfig;
<h1>Counter</h1>

<p>Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }

    //(IDataAccess _db, string mainGoal) = _startConfig.GetProgressDbConfig();

    //List<ProjectProgressLibrary.Models.ProjectModel> allProjects = _db.ReadAllProjectRecords();
}
